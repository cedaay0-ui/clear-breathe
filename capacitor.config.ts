import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.lovable.smokefree",
  appName: "SmokeFree",
  webDir: "dist/client",
};

export default config;
